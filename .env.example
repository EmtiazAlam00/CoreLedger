DATABASE_URL=postgresql+psycopg://coreledger:coreledger@localhost:5432/coreledger

# Free sandbox credentials from https://dashboard.plaid.com — used only by
# scripts/ingest_plaid_seed_data.py, never by the API/ledger_writer.
PLAID_CLIENT_ID=
PLAID_SECRET=
